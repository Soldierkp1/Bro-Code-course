#Typecasting = the process of converting a variable from one type of data into another.
#              str(), int(), float(), bool().


name = "Ale"
age = 25
gpa = 4.0
is_student = True

#type() = to know the type of a variable, needs to be inside a "print()".

#Typecasting bool() on a string, is usefull to check if someone has typed their name or not, since it will return False, if the input is empty.
